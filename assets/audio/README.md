# Add campaign music here

The included WAV files are short, looping-friendly ambience samples. Replace them with your own MP3, OGG, or WAV files, then update the matching `file` value in `js/data.js`.

The dashboard's included tracks are named `oakhaven.wav`, `tavern.wav`, `forest.wav`, `mystery.wav`, and `combat.wav`.
